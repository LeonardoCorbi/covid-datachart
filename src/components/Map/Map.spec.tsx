import { render } from '@testing-library/react';
import Map from '.';

describe('Map component', () => {
  it('should render correctly', () => {
    // render(
    //   <Map
    //     tooltip='Teste'
    //     method='partial'
    //     countryData={{}}
    //     setTooltip={() => {} as Dispatch<SetStateAction<string>>}
    //     setShowToolTip={() => {} as Dispatch<SetStateAction<string>>}
    //   />
    // );
  });
});